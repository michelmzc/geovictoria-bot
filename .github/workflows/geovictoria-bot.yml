name: Geovictoria Bot Test

on:
  # Ejecutar manualmente desde GitHub UI
  workflow_dispatch:

  # Para pruebas, puedes agregar un cron que corra cada día a las 9am UTC (ajusta horario)
  #schedule:
  #  - cron: '0 9 * * *'  

jobs:
  test-login:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Setup Python 3.12
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Geovictoria bot
      env:
        USER_EMAIL: ${{ secrets.GEO_EMAIL }}
        USER_PASS: ${{ secrets.GEO_PASS }}
      run: python bot.py
